#include "map_tree.h"
#include "bin_tree.h"
#include <vector>

template <typename T>
void tree_sort(std::vector<T> &v){
    //Creamos un par elemento, numRepeticiones
    //MapEntry<T,int> entry = MapEntry(v[0],1);
    //Asignamos el primer elemento a un arbol
    MapTree<T> mapa = MapTree<T>();
    mapa.insert(entry);
    
}

int main(){

}